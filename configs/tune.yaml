model_name: Qwen/Qwen2.5-0.5B-Instruct
dtype: bfloat16
device: cuda
adapter_rank: 16
adapter_layers: 4
adapter_scale: 1.0
action_count: 4
task_variant: codebook
prompt_mode: disclosed
split_seed: 0
methods: [base, bp_grpo, fo_pg, fo_npg, focus_npg]
seeds: [0]
steps: 40
batch_size: 16
group_size: 16
eval_interval: 2
baseline: loo
rollout_kl_beta: 0.0
calibration_examples: 64
wandb_mode: offline
backprop:
  learning_rate: 0.01
  epochs_per_rollout: 2
  clip_epsilon: 0.2
  max_grad_norm: 1.0
forward:
  directions: 8
  finite_difference_mu: 0.5
  fisher_damping: 0.1
  kl_budget: 0.02
  clip_epsilon: 0.2
  max_step_norm: 5.0
  line_search_steps: 6
  active_rank: 4
  history_size: 16
  es_sigma: 0.5
  es_step_norm: 0.5
